% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{fps_format_md}
\alias{fps_format_md}
\title{FPS: Format Markdown files for GOV.UK}
\usage{
fps_format_md(
  out_dir_md = ".",
  out_dir_figs = ".",
  section = NULL,
  chart_ext = "svg",
  date = tolower(format(Sys.Date(), "\%d\%b\%y"))
)
}
\arguments{
\item{out_dir_md}{Character. The directory containing the markdown files.
Defaults to the current directory (\code{"."}).}

\item{out_dir_figs}{Character. The directory containing the image files.
Defaults to the current directory (\code{"."}).}

\item{section}{Character (optional). A specific subdirectory or section
within the output directories to process. Defaults to \code{NULL}.}

\item{chart_ext}{Character. The file extension of chart images (e.g. "svg",
"png"). Defaults to \code{"svg"}.}

\item{date}{Character. The date string used to filter \code{.md} files. Defaults
to today's date in the format \code{"\%d\%b\%y"} (e.g. "14jan25").}
}
\value{
None. The function modifies \code{.md} files in place as a side effect.
}
\description{
This function processes \code{.md} files in a specified output
directory, removing YAML headers and replacing image tags to conform to
GOV.UK formatting standards.
}
\details{
\itemize{
\item YAML headers in the \code{.md} files are removed.
\item Image tags are updated to match GOV.UK standards.
\item Only \code{.md} (non-README) files created on the specified date are processed.
}
}
\examples{
#fps_format_md(out_dir_md = "output/report", out_dir_figs = "output/charts", section = "section_name")

}
\author{
Tom Pearson
}
